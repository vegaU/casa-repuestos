# Servicios de la aplicación (se añadirán más adelante).
services: {}

# Red interna compartida por los servicios de la aplicación.
networks:
  app-network:

# Volumen persistente para los datos de PostgreSQL.
volumes:
  postgres-data:
